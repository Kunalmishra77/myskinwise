import { Section } from "@/components/ui/section";

/**
 * Temporary placeholder for the marketing home page so `/` resolves inside
 * the marketing chrome (Navbar/StickyCTA/Footer). The real Home page
 * content is built in a later task.
 */
export default function HomePage() {
  return (
    <Section>
      <h1 className="font-serif text-4xl font-semibold text-ink">Skinwise</h1>
    </Section>
  );
}
