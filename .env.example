# No environment variables are required to run the marketing site locally yet.
# This file documents required variables (never real values) as they are introduced
# in later tasks (e.g. Supabase keys for lead capture / admin).
